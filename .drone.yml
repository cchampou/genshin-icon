kind: pipeline
type: docker
name: default

steps:
  - name: lint
    image: node
    commands:
    - yarn
    - yarn lint
  - name: build & publish
    image: node
    commands:
    - yarn
    - yarn publish
#    when:
#      event: tag
#      branch: master